package com.qike.corelibrary.net;

import com.qike.corelibrary.net.domain.Pager;
/**
 * 
 *<p>带分页的一个回掉借口</p><br/>
 * @since 1.0.0
 * @author xky
 * @param <T>
 */
public interface IPagerBaseCallBack<T> {
	/**
	 * 
	 *<p>请求完成</p><br/>
	 *<p>TODO(详细描述)</p>
	 * @since 1.0.0
	 * @author xky
	 * @param pager 分页对象
	 * @param isSuccess 是否成功
	 * @param isCache	是否拿的缓存
	 * @param result  结果
	 */
	public void onFinish(Pager pager,boolean isSuccess,boolean isCache,T result);
}
